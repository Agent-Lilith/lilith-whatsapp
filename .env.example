# PostgreSQL (same cluster as other Lilith apps; database name: lilith_whatsapp)
# Must be a dedicated DB for this project (see README: "Can't locate revision 002_unique").
DATABASE_URL=postgresql://user:pass@localhost:5432/lilith_whatsapp

# Embedding service URL (required). Only the Python MCP server uses this for vector search.
# The Bun connector does not call the embedding service; it only writes chats/contacts/messages.
EMBEDDING_URL=http://localhost:6003
